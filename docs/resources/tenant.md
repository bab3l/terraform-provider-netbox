---
page_title: "netbox_tenant Resource - terraform-provider-netbox"
subcategory: ""
description: |-
  Manages a tenant in Netbox. Tenants represent individual customers or organizational units in multi-tenancy scenarios, allowing you to organize and track resources by client or department.
---

# netbox_tenant (Resource)

Manages a tenant in Netbox. Tenants represent individual customers or organizational units in multi-tenancy scenarios, allowing you to organize and track resources by client or department.

## Example Usage

```terraform
terraform {
  required_providers {
    netbox = {
      source = "local/bab3l/netbox"
    }
  }
}

provider "netbox" {
  server_url = "https://netbox.example.com"
  api_token  = "your_api_token_here"
}

# Create a tenant group first
resource "netbox_tenant_group" "example_group" {
  name        = "Example Tenant Group"
  slug        = "example-tenant-group"
  description = "A group for organizing tenants"
}

# Create a tenant within the group
resource "netbox_tenant" "example_tenant" {
  name        = "Example Tenant"
  slug        = "example-tenant"
  group       = netbox_tenant_group.example_group.slug
  description = "An example tenant"
  comments    = "This tenant is used for demonstration purposes"

  # Partial custom fields management
  # Only specified custom fields are managed, others preserved
  custom_fields = [
    {
      name  = "account_id"
      value = "ACCT-12345"
    },
    {
      name  = "billing_contact"
      value = "billing@example.com"
    },
    {
      name  = "contract_end_date"
      value = "2026-12-31"
    },
    {
      name  = "support_tier"
      value = "premium"
    }
  ]

  tags = [
    {
      name = "production"
      slug = "production"
    },
    {
      name = "critical"
      slug = "critical"
    }
  ]
}

# Create a tenant without a group
resource "netbox_tenant" "standalone_tenant" {
  name        = "Standalone Tenant"
  slug        = "standalone-tenant"
  description = "A tenant without a group"
}

# Optional: seed owned custom fields during import
import {
  to = netbox_tenant.example_tenant
  id = "123"

  identity = {
    custom_fields = [
      "account_id:text",
      "billing_contact:text",
      "contract_end_date:date",
      "support_tier:text",
    ]
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the tenant.
- `slug` (String) URL-friendly identifier for the tenant. Must be unique and contain only lowercase letters, numbers, hyphens, and underscores.

### Optional

- `comments` (String) Additional comments or notes about the tenant. Supports Markdown formatting.
- `custom_fields` (Attributes Set) Custom fields assigned to this resource. Custom fields must be defined in Netbox before use. (see [below for nested schema](#nestedatt--custom_fields))
- `description` (String) Description of the tenant.
- `group` (String) ID or slug of the tenant group that this tenant belongs to.
- `tags` (Set of String) Tags assigned to this resource. Tags must already exist in Netbox.

### Read-Only

- `id` (String) Unique identifier for the tenant (assigned by Netbox).

<a id="nestedatt--custom_fields"></a>
### Nested Schema for `custom_fields`

Required:

- `name` (String) Name of the custom field.
- `type` (String) Type of the custom field (text, longtext, integer, boolean, date, url, json, select, multiselect, object, multiobject).
- `value` (String) Value of the custom field.

## Import

Import is supported using the following syntax:

```shell
# Tenants can be imported by ID
terraform import netbox_tenant.example_tenant 123
```
