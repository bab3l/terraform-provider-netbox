---
page_title: "netbox_ipsec_profile Resource - terraform-provider-netbox"
subcategory: ""
description: |-
  Manages an IPSec Profile in Netbox. IPSec profiles combine IKE and IPSec policies to define complete VPN configurations.
---

# netbox_ipsec_profile (Resource)

Manages an IPSec Profile in Netbox. IPSec profiles combine IKE and IPSec policies to define complete VPN configurations.

## Example Usage

```terraform
resource "netbox_ike_proposal" "test" {
  name                     = "Test IKE Proposal"
  authentication_method    = "pre-shared-keys"
  encryption_algorithm     = "aes-256-cbc"
  authentication_algorithm = "sha-256"
  group                    = "group-14"
}

resource "netbox_ike_policy" "test" {
  name      = "Test IKE Policy"
  version   = "v2"
  mode      = "main"
  proposals = [netbox_ike_proposal.test.id]
}

resource "netbox_ipsec_proposal" "test" {
  name                     = "Test IPSec Proposal"
  encryption_algorithm     = "aes-256-cbc"
  authentication_algorithm = "sha-256"
}

resource "netbox_ipsec_policy" "test" {
  name      = "Test IPSec Policy"
  proposals = [netbox_ipsec_proposal.test.id]
}

resource "netbox_ipsec_profile" "test" {
  name         = "Test IPSec Profile"
  mode         = "esp"
  ike_policy   = netbox_ike_policy.test.name
  ipsec_policy = netbox_ipsec_policy.test.name
}

import {
  to = netbox_ipsec_profile.test
  id = "123"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `ike_policy` (String) The name of the IKE policy to use. Required.
- `ipsec_policy` (String) The name of the IPSec policy to use. Required.
- `mode` (String) The IPSec mode. Required. Valid values: `esp` (Encapsulating Security Payload), `ah` (Authentication Header).
- `name` (String) The name of the IPSec profile. Required.

### Optional

- `comments` (String) Additional comments or notes about the IPSec profile. Supports Markdown formatting.
- `custom_fields` (Attributes Set) Custom fields assigned to this resource. Custom fields must be defined in Netbox before use. (see [below for nested schema](#nestedatt--custom_fields))
- `description` (String) Description of the IPSec profile.
- `tags` (Set of String) Tags assigned to this resource. Tags must already exist in Netbox.

### Read-Only

- `id` (String) The unique numeric ID of the IPSec profile.

<a id="nestedatt--custom_fields"></a>
### Nested Schema for `custom_fields`

Required:

- `name` (String) Name of the custom field.
- `type` (String) Type of the custom field (text, longtext, integer, boolean, date, url, json, select, multiselect, object, multiobject).
- `value` (String) Value of the custom field.

## Import

Import is supported using the following syntax:

```shell
# IPSec Profiles can be imported by ID
terraform import netbox_ipsec_profile.test 123
```
