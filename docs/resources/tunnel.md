---
page_title: "netbox_tunnel Resource - terraform-provider-netbox"
subcategory: ""
description: |-
  Manages a VPN tunnel in Netbox. Tunnels represent secure connections between network endpoints using various encapsulation protocols like IPSec, GRE, WireGuard, etc.
---

# netbox_tunnel (Resource)

Manages a VPN tunnel in Netbox. Tunnels represent secure connections between network endpoints using various encapsulation protocols like IPSec, GRE, WireGuard, etc.

## Example Usage

```terraform
resource "netbox_tunnel" "test" {
  name          = "Test Tunnel"
  encapsulation = "ipsec-tunnel"
  status        = "active"
  description   = "IPSec tunnel between datacenter sites"
  comments      = "Primary site-to-site VPN tunnel"

  # Partial custom fields management
  # Only specified custom fields are managed, others in NetBox preserved
  custom_fields = [
    {
      name  = "tunnel_id"
      value = "TUN-001"
    },
    {
      name  = "encryption_algorithm"
      value = "AES-256-GCM"
    },
    {
      name  = "preshared_key_rotation_days"
      value = "90"
    }
  ]

  tags = [
    "ipsec",
    "site-to-site"
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `encapsulation` (String) Encapsulation protocol for the tunnel. Valid values: `ipsec-transport`, `ipsec-tunnel`, `ip-ip`, `gre`, `wireguard`, `openvpn`, `l2tp`, `pptp`.
- `name` (String) Name of the tunnel.

### Optional

- `comments` (String) Additional comments or notes about the tunnel. Supports Markdown formatting.
- `custom_fields` (Attributes Set) Custom fields assigned to this resource. Custom fields must be defined in Netbox before use. (see [below for nested schema](#nestedatt--custom_fields))
- `description` (String) Description of the tunnel.
- `group` (String) ID of the tunnel group this tunnel belongs to.
- `ipsec_profile` (String) ID of the IPSec profile for this tunnel (required for IPSec encapsulation types).
- `status` (String) Operational status of the tunnel. Valid values: `planned`, `active`, `disabled`.
- `tags` (Attributes Set) Tags assigned to this resource. Tags must already exist in Netbox. (see [below for nested schema](#nestedatt--tags))
- `tenant` (String) ID of the tenant this tunnel belongs to.
- `tunnel_id` (Number) Tunnel identifier (numeric ID used by the tunnel protocol).

### Read-Only

- `id` (String) Unique identifier for the tunnel (assigned by Netbox).

<a id="nestedatt--custom_fields"></a>
### Nested Schema for `custom_fields`

Required:

- `name` (String) Name of the custom field.
- `type` (String) Type of the custom field (text, longtext, integer, boolean, date, url, json, select, multiselect, object, multiobject).
- `value` (String) Value of the custom field.


<a id="nestedatt--tags"></a>
### Nested Schema for `tags`

Required:

- `name` (String) Name of the existing tag.
- `slug` (String) Slug of the existing tag.

## Import

Import is supported using the following syntax:

```shell
# Tunnels can be imported by ID
terraform import netbox_tunnel.test 123
```
