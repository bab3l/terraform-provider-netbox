---
page_title: "netbox_ip_range Resource - terraform-provider-netbox"
subcategory: ""
description: |-
  Manages an IP address range in Netbox. IP ranges are used to define a contiguous range of IP addresses within a prefix.
---

# netbox_ip_range (Resource)

Manages an IP address range in Netbox. IP ranges are used to define a contiguous range of IP addresses within a prefix.

## Example Usage

```terraform
resource "netbox_ip_range" "test" {
  start_address = "10.0.0.1/24"
  end_address   = "10.0.0.10/24"
  status        = "active"
  description   = "DHCP pool for guest WiFi"
  comments      = "Reserved IP range for dynamic allocation"

  # Partial custom fields management
  # Only specified custom fields are managed, others in NetBox preserved
  custom_fields = [
    {
      name  = "range_purpose"
      value = "dhcp-pool"
    },
    {
      name  = "allocation_type"
      value = "dynamic"
    },
    {
      name  = "lease_time_hours"
      value = "24"
    }
  ]

  tags = [
    "dhcp",
    "guest-network"
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `end_address` (String) The ending IP address of the range (e.g., 192.168.1.20/24).
- `start_address` (String) The starting IP address of the range (e.g., 192.168.1.10/24).

### Optional

- `comments` (String) Additional comments or notes about the IP range. Supports Markdown formatting.
- `custom_fields` (Attributes Set) Custom fields assigned to this resource. Custom fields must be defined in Netbox before use. (see [below for nested schema](#nestedatt--custom_fields))
- `description` (String) Description of the IP range.
- `mark_utilized` (Boolean) Treat this range as fully utilized regardless of actual usage. Defaults to `false`.
- `role` (String) The name or ID of the IPAM role for this IP range.
- `status` (String) The status of the IP range. Valid values are: `active`, `reserved`, `deprecated`. Defaults to `active`.
- `tags` (Attributes Set) Tags assigned to this resource. Tags must already exist in Netbox. (see [below for nested schema](#nestedatt--tags))
- `tenant` (String) The name or ID of the tenant this IP range is assigned to.
- `vrf` (String) The name or ID of the VRF this IP range is assigned to.

### Read-Only

- `id` (String) The unique numeric ID of the IP range.
- `size` (Number) The number of IP addresses in the range (computed).

<a id="nestedatt--custom_fields"></a>
### Nested Schema for `custom_fields`

Required:

- `name` (String) Name of the custom field.
- `type` (String) Type of the custom field (text, longtext, integer, boolean, date, url, json, select, multiselect, object, multiobject).
- `value` (String) Value of the custom field.


<a id="nestedatt--tags"></a>
### Nested Schema for `tags`

Required:

- `name` (String) Name of the existing tag.
- `slug` (String) Slug of the existing tag.

## Import

Import is supported using the following syntax:

```shell
# Order can be imported by specifying the numeric identifier.
terraform import netbox_ip_range.test 1
```
