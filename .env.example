# Terraform Provider Netbox - Test Environment Configuration
# Copy this file to .env and customize as needed

# Netbox connection settings for acceptance tests
NETBOX_SERVER_URL=http://localhost:8000
NETBOX_API_TOKEN=0123456789abcdef0123456789abcdef01234567

# Enable Terraform acceptance tests
TF_ACC=1

# Optional: Skip TLS verification (useful for self-signed certs)
# NETBOX_INSECURE=true

# Optional: Test timeout (default is 120m)
# TEST_TIMEOUT=120m

# Docker Compose Netbox configuration
# These match the docker-compose.yml defaults
POSTGRES_DB=netbox
POSTGRES_USER=netbox
POSTGRES_PASSWORD=netbox
SUPERUSER_NAME=admin
SUPERUSER_EMAIL=admin@example.com
SUPERUSER_PASSWORD=admin
